Ninja Build:
    ninja-build (apt, dnf, pacman, apk, pip)

Libcurl:
    libcurl4-openssl-dev(apt) # Include OpenSSL
    libcurl-devel(yum, dnf)
    curl(pacman, brew, vcpkg)

OpenSSL:
    openssl(apt, dnf)
